'use client';
import Mapbox from '@/components/ui/Mapbox/Mapbox';
import React from 'react';

const MapPage = () => {
    return (
        <>
             <Mapbox />
        </>
    );
};

export default MapPage;